# ============================================
# 通用环境配置（所有环境共享）
# ============================================
# 注意：只有以 VITE_ 开头的变量才会暴露给客户端代码

# 应用名称
VITE_APP_NAME=Lumina

# 应用版本（可以在构建时动态注入）
VITE_APP_VERSION=1.0.0
